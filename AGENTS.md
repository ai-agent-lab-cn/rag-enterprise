# RAG Enterprise 项目规则

## 事实与表达

- GitHub Issue、PR、commit、CI、Tag 和部署记录是工程事实源；不要在任务中复制维护代码状态。
- 面向用户的 Issue、PR、commit、CI 步骤和进度说明默认使用中文；必要技术标识保留英文。
- 修改前检查当前分支和工作区，保留用户已有改动，不清理、不重置、不覆盖无关文件。

## 阶段边界

- RAG 能力是 V0→V4 主线，CI/CD 和页面只是阶段验收能力。
- 未收到明确启动指令前，不修改阶段代码；未经上一阶段放行，不实施下一阶段。
- 设计图中的功能不等于实施授权。先按当前阶段分为本轮实现、保留现状、未来阶段和纯视觉参考。
- 当前任务未授权时，不增加多知识库、历史会话、回答评测、登录、权限、系统运维或审计能力。

## UI 设计与 Vibe Coding

- 上传设计图、参考图或要求调整 UI 时，使用 `$rag-ui-vibe-workflow`。
- 完整读取该 Skill 和项目基线 `docs/design/ui-baseline.md` 后再修改页面；项目基线优先于设计图中的偶然差异。
- 先读取现有组件、样式、类型、接口和测试；优先复用，禁止无理由整页重写。
- 每轮只处理一个页面或一组强相关区域；已确认区域视为冻结，不随其他调整反复改动。
- 设计稿只提供视觉与交互参考，不得用假按钮、假数字或静态状态冒充尚未实现的功能。
- 保留现有业务逻辑、真实数据状态、错误处理、可访问性和测试。

## 页面完成标准

- 运行前端测试、ESLint 和生产构建；后端或接口变更同时运行对应后端质量门。
- 能启动应用时，必须检查真实页面并完成桌面端与移动端截图对比。
- 覆盖空、加载、正常和失败状态；Hover 不引发布局抖动，抽屉和弹窗支持减少动画。
- 交付时说明实现范围、排除的未来阶段功能、验证结果和仍存在的视觉差异。
- 代码生成或测试通过不等于页面完成；必须以可运行页面和视觉检查为准。
