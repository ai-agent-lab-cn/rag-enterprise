# 索引治理页面优化实施计划

> 用途：供 Claude Code 直接实施\
> 范围：知识库详情页中的「索引治理」区域\
> 核心目标：不做二级 Tab、紧凑展示、优化状态与治理术语文案。

---

# 1. 改造目标

本次不重做索引治理业务模型，只调整页面信息架构、交互层级和 UI 文案。

重点调整：

1. 索引治理内部不做二级 Tab。
2. 「索引版本」「正式质量评测」「运行记录」在一个页面中直接展开。
3. 页面整体紧凑展示，减少大卡片和大面积留白。
4. 优化 `Legacy`、`冻结发布证据`、`Config Fingerprint`、`validation_failed` 等工程化文案。
5. 主页面突出：
   - 当前线上版本是谁
   - 候选版本进行到哪一步
   - 为什么不能发布
   - 下一步应该执行什么

---

# 2. 一级导航调整

当前：

```text
资料
数据源
分类管理
版本治理
权限边界
会话
```

建议调整为：

```text
资料
数据源
数据同步
分类管理
权限边界
索引治理
会话
```

原「版本治理」中的内容重新归属：

```text
资料
└── 资料版本

数据同步
└── 文件更新
└── 数据同步任务
└── Sync Run
└── Retry / Dead Letter

索引治理
├── 索引版本
├── 正式质量评测
└── 索引运行记录
```

本次重点实施「索引治理」。

---

# 3. 索引治理页面整体结构

改成一个页面直接展开：

```text
索引治理

发布流程摘要

当前索引定义

索引版本

正式质量评测

运行记录
```

推荐页面顺序：

```text
1. 页面标题与主操作
2. 发布流程摘要
3. 当前索引定义
4. 索引版本
5. 正式质量评测
6. 运行记录
```

逻辑：

```text
当前配置是什么
      ↓
现在有哪些索引版本
      ↓
候选版本质量是否达标
      ↓
最近系统执行了什么
```

---

# 4. 页面头部

推荐：

```text
索引治理                                      [+ 创建索引版本]
管理索引版本的构建、质量验证、发布与回滚。
```

说明文字只保留一行。

不要增加 Dashboard 式 KPI：

```text
版本总数 12
运行中 2
失败 3
报告 18
```

目前这些信息不是主任务。

---

# 5. 发布流程摘要

原：

```text
Definition → Version → Build → Validate → Activate
```

改成：

```text
索引定义 → 版本 → 构建 → 正式评测 → 发布验证 → 待激活 → 生效
```

推荐紧凑展示：

```text
索引定义  →  版本  →  构建  →  正式评测  →  发布验证  →  待激活  →  生效
   ✓          ✓        ✓          ✓             ●            ○         ○
```

状态：

```text
✓ 已完成
● 进行中
○ 未开始
! 需要处理
× 未通过
```

注意：

**正式评测和发布验证必须明确为两个阶段。**

即：

```text
Build
  ↓
正式检索评测
  ↓
生成正式质量报告
  ↓
发布验证
  ├─ 完整性检查
  ├─ 技术检查
  └─ 检索质量检查
  ↓
ready
  ↓
Activate
```

---

# 6. 当前索引定义

不要做大卡片。

推荐紧凑展示：

```text
当前索引定义

Parser 1.0 · Chunk v1-700-100 · text2vec-base-chinese · 768 维
配置已更新，新建索引版本后生效。                         [查看配置]
```

如果配置未变化：

```text
当前配置与最新索引版本一致。
```

如果配置变化：

```text
配置已更新
新建索引版本后生效。
```

主页面不要直接展示完整 SHA-256：

```text
7b9a15844dd07ffffd65d465ecafb9b58081a76d73a77e1d4fcfdaf830563810
```

完整值进入详情。

---

# 7. 索引版本

标题：

```text
索引版本                                      [+ 创建索引版本]
```

推荐表格：

| 版本 | 状态   | 当前阶段   | 配置摘要                              | 质量状态    | 创建时间           | 操作   |
| -- | ---- | ------ | --------------------------------- | ------- | -------------- | ---- |
| v3 | 验证中  | 检索质量检查 | 1.0 · v1-700-100 · text2vec · 768 | 正式评测已通过 | 2026/9/7 22:39 | 查看验证 |
| v2 | 构建失败 | 索引构建   | 历史配置不可完整追溯                        | —       | 2026/9/6 09:37 | 重新构建 |
| v1 | 当前生效 | 线上生效   | 历史配置不可完整追溯                        | 历史版本    | 2026/9/6 09:07 | 详情   |

---

# 8. 状态与阶段分离

不要只显示：

```text
validating
```

应该表达成：

```text
状态
验证中

当前阶段
检索质量检查
```

或者在表格中：

```text
验证中
检索质量检查
```

生命周期状态回答：

> 这个版本处于什么状态？

当前阶段回答：

> 它现在具体执行到哪里？

---

# 9. 状态文案统一

后端枚举保留，不要求修改。

前端统一映射：

| 后端状态               | UI 文案 | 说明        |
| ------------------ | ----- | --------- |
| draft              | 草稿    | 尚未开始构建    |
| building           | 构建中   | 正在生成索引    |
| build\_failed      | 构建失败  | 构建过程需要处理  |
| validating         | 验证中   | 正在执行发布验证  |
| validation\_failed | 验证未通过 | 部分发布条件未满足 |
| ready              | 待激活   | 已满足发布条件   |
| active             | 当前生效  | 当前线上使用版本  |
| previous           | 上一版本  | 上一个线上生效版本 |
| retired            | 已退役   | 已停止作为候选版本 |
| cleaned            | 已清理   | 索引数据已释放   |

主 UI 禁止直接出现：

```text
build_failed
validation_failed
previous
retired
cleaned
```

---

# 10. 操作按钮收敛

当前版本列表可能存在：

```text
详情
执行验证
重新构建
清理
```

按钮过多。

每行最多：

```text
1 个主操作
+
1 个 ···
```

状态对应主操作：

| 状态    | 主操作    |
| ----- | ------ |
| 草稿    | 开始构建   |
| 构建中   | 查看进度   |
| 构建失败  | 重新构建   |
| 验证中   | 查看验证   |
| 验证未通过 | 重新验证   |
| 待激活   | 激活版本   |
| 当前生效  | 查看详情   |
| 上一版本  | 回滚到此版本 |
| 已退役   | 清理     |
| 已清理   | 查看记录   |

更多操作放：

```text
···
```

例如：

```text
查看构建日志
重新构建
查看验证报告
退役
清理
```

---

# 11. 正式质量评测

不要做 Tab。

直接放在索引版本下方：

```text
正式质量评测                                  [运行正式评测]
用于确认候选索引的检索质量，并作为发布验证依据。
```

推荐表格：

| 报告      | 版本 | 评测配置    | 结果  | Recall\@10 |  MRR | nDCG\@10 | 发布用途   | 时间    |
| ------- | -- | ------- | --- | ---------: | ---: | -------: | ------ | ----- |
| qr\_102 | v3 | 与当前版本一致 | 已通过 |       0.93 | 0.79 |     0.84 | 可用于发布  | 22:35 |
| qr\_101 | v3 | 配置已变化   | 已通过 |       0.91 | 0.77 |     0.82 | 不可用于发布 | 21:02 |

不要显示：

```text
Config Fingerprint Match
Fingerprint mismatch
Legacy
```

主 UI 改成：

```text
与当前版本一致
配置已变化
历史报告
```

完整 Config Fingerprint 进入报告详情。

---

# 12. 缺少正式质量报告

当前文案类似：

```text
正式质量报告暂无配置指纹匹配的已通过报告

请先使用本版本配置运行正式检索评测。
其他版本或旧版无配置指纹的报告不能用于放行。
```

改成：

```text
缺少可用于发布的质量报告

当前版本尚未完成符合发布要求的正式检索评测。
请先运行正式评测，通过后即可继续发布验证。

[运行正式评测]
```

辅助说明：

```text
只有使用当前版本配置生成且结果通过的正式报告，
才能作为本版本的发布依据。
```

技术原因不要放在主错误文案第一层。

---

# 13. 运行记录

直接放页面最下面：

```text
运行记录
```

只展示索引治理任务：

```text
索引构建
正式质量评测
发布验证
激活
回滚
重新构建
退役
清理
```

移除：

```text
文件上传
文件更新
数据同步
```

这些属于资料或数据同步。

推荐字段：

| 类型     | 版本 | 当前阶段   |   进度 | 状态  | 开始时间  | 耗时    | 操作 |
| ------ | -- | ------ | ---: | --- | ----- | ----- | -- |
| 发布验证   | v3 | 检索质量检查 |  95% | 运行中 | 22:39 | 1m12s | 详情 |
| 正式质量评测 | v3 | 已完成    | 100% | 已完成 | 22:34 | 4m31s | 详情 |
| 索引构建   | v3 | 已完成    | 100% | 已完成 | 22:28 | 5m52s | 详情 |

删除：

```text
处理数量 0/1
处理数量 1/1
```

这个字段更适合 Sync Pipeline。

---

# 14. 索引版本详情页

版本详情页结构：

```text
版本标题

发布流程

版本信息 + 正式质量评测

发布验证

发布记录

相关运行记录
```

---

# 15. 版本详情页头

推荐：

```text
← 索引版本

v3                                             验证中
iv_eab6a49c11b143e7

当前阶段：检索质量检查

                                      [重新构建] [···]
```

不要出现：

```text
validating
```

---

# 16. 版本详情发布流程

```text
构建完成 → 正式评测 → 发布验证 → 待激活 → 当前生效
   ✓          ✓           ●          ○          ○
```

保持紧凑。

不要做很高的大型 Steps 卡片。

---

# 17. 版本信息 + 正式质量评测

推荐双栏：

```text
版本信息                              正式质量评测

Parser       1.0                     结果          已通过
Chunk        v1-700-100              Recall@5      0.86
Embedding    text2vec-base-chinese   Recall@10     0.93
维度         768                     MRR           0.79
Documents    1,283                   nDCG@10       0.84
Chunks       8,271                   报告          qr_xxx [查看]
```

主页面不要突出 Config Fingerprint。

可以显示：

```text
配置快照
已记录
```

点击详情后才展示 Hash。

---

# 18. 发布验证

区域标题使用：

```text
发布验证
```

不要使用：

```text
Validation Gate
验证索引版本门禁
```

三层检查紧凑横排：

```text
✓ 完整性检查       ✓ 技术检查       ● 检索质量检查
  4 / 4              5 / 5             进行中
```

必要时点击展开。

---

# 19. 完整性检查详情

包含：

```text
Document Coverage
Chunk Consistency
ACL Consistency
Missing Document
Orphan Chunk
Duplicate Chunk
```

主 UI 可翻译：

```text
文档完整性
Chunk 一致性
ACL 一致性
缺失文档
孤立 Chunk
重复 Chunk
```

---

# 20. 技术检查详情

包含：

```text
Vector
Metadata
ACL
Citation
Version
```

主 UI：

```text
向量索引
Metadata
权限数据
引用定位
版本一致性
```

---

# 21. 检索质量检查

包含：

```text
Recall@5
Recall@10
MRR
nDCG@10
```

这一层引用正式质量报告作为发布依据。

逻辑：

```text
正式质量报告
   ↓
配置与当前版本一致
   ↓
结果已通过
   ↓
检索质量检查通过
```

---

# 22. 门禁状态文案

不要使用：

```text
PASS
FAIL
BLOCKED
PENDING
```

主 UI 映射：

| 技术状态    | UI     |
| ------- | ------ |
| PASS    | 已通过    |
| FAILED  | 未通过    |
| RUNNING | 检查中    |
| BLOCKED | 等待前置条件 |
| PENDING | 未开始    |

例如：

```text
检索质量检查
等待前置条件

缺少可用于发布的正式质量报告。

[运行正式评测]
```

不要显示：

```text
BLOCKED: config fingerprint report missing
```

---

# 23. 验证成功状态

三层全部通过：

```text
发布验证

✓ 所有发布检查均已通过

版本状态
待激活

验证报告
vr_bff355...                                  [查看报告]

验证时间
2026/9/7 22:46

验证通过不会自动切换线上版本。

                                           [激活版本]
```

核心规则：

```text
验证通过
   ↓
ready
   ↓
用户主动 Activate
   ↓
active
```

不要自动激活。

---

# 24. “冻结发布证据”重命名

当前：

```text
冻结发布证据
配置指纹
7b9a...
发布指纹
Legacy 未生成
```

不建议继续保留。

问题：

1. “冻结”是内部治理术语。
2. Hash 对主页面用户没有直接决策价值。
3. `Legacy 未生成` 容易误认为错误。
4. 页面像数据库字段展示。

主 UI 改为：

```text
发布记录
```

---

# 25. 发布记录主视图

推荐：

```text
发布记录                                      [查看详情]

配置快照          已记录
质量报告          已通过
验证报告          已通过
线上激活          尚未执行
```

如果已经发布：

```text
发布记录                                      [查看详情]

配置快照          已记录
质量报告          qr_xxx
验证报告          vr_xxx
线上激活          2026/9/7 22:58
```

不要在主视图直接显示：

```text
Config Fingerprint
Release Fingerprint
完整 SHA-256
```

---

# 26. 发布记录详情

点击「查看详情」：

```text
发布记录详情

配置快照
已冻结

配置指纹
7b9a1584...563810                            [复制]

质量报告
qr_xxx                                       [查看]

验证报告
vr_bff355...                                 [查看]

发布指纹
72fa...91bc                                  [复制]

激活时间
2026/9/7 22:58

操作人
admin
```

技术审计能力保留。

只是从主页面降级。

---

# 27. Legacy 文案

不要主 UI 展示：

```text
Legacy
Legacy 未生成
旧版无配置指纹
```

统一使用：

```text
历史版本
```

或：

```text
历史迁移
```

例如：

```text
配置
历史配置不可完整追溯
```

```text
发布记录
历史版本 · 部分信息未记录
```

Tooltip / Detail：

```text
该版本创建于当前索引治理机制启用之前，
因此部分配置、质量报告或发布记录未被保存。
```

---

# 28. 发布指纹历史版本

原：

```text
发布指纹
Legacy 未生成
```

改为主 UI：

```text
发布记录
历史版本 · 部分信息未记录
```

详情：

```text
发布指纹
—

该历史版本发布时尚未启用发布指纹机制。
```

历史数据缺失不是错误。

---

# 29. Config Fingerprint 文案

技术字段继续存在。

但主页面：

```text
配置快照
已记录
```

详情：

```text
配置指纹
7b9a15844dd07ffffd65d465ecafb9b58081a76d73a77e1d4fcfdaf830563810
```

质量报告主 UI：

```text
评测配置
与当前版本一致
```

而不是：

```text
Config Fingerprint Match
```

---

# 30. “绑定验证报告”

不要展示：

```text
绑定验证报告
vr_bff355b72b974068a5d6
```

改为：

```text
验证报告
已通过 · vr_bff3...a5d6                    [查看]
```

或者更紧凑：

```text
验证报告       已通过                       [查看]
```

完整 ID 进入详情。

---

# 31. 紧凑化设计规则

这是本次 UI 实施重点。

## Section

推荐间距：

```text
Section 间距：20~24px
标题与内容：8~12px
```

不要：

```text
40~64px 大面积纵向留白
```

---

# 32. 卡片

避免：

```text
一个指标一个 Card
一个门禁一个巨大 Card
一个状态一个大色块
```

推荐：

```text
section
divider
compact bordered group
table
description list
```

Card padding：

```text
12~16px
```

---

# 33. Table Density

推荐：

```text
Row Height：40~48px
```

不要过大的：

```text
56~72px
```

版本 ID 可作为版本名下面的 secondary text。

例如：

```text
v3
iv_eab6...43e7
```

---

# 34. ID 与 Hash 展示规则

统一缩写：

```text
iv_eab6...43e7
vr_bff3...a5d6
7b9a1584...63810
```

旁边支持：

```text
复制
```

完整值只在：

```text
Tooltip
Drawer
Detail
```

中显示。

---

# 35. 最终索引治理主页面草图

```text
索引治理                                                   [+ 创建索引版本]
管理索引版本的构建、质量验证、发布与回滚。

索引定义 → 版本 → 构建 → 正式评测 → 发布验证 → 待激活 → 生效
   ✓         ✓      ✓        ✓           ●          ○        ○

当前索引定义
Parser 1.0 · Chunk v1-700-100 · text2vec-base-chinese · 768维
配置已更新，新建版本后生效。                                  [查看配置]

──────────────────────────────────────────────────────────────

索引版本

版本      状态        当前阶段          配置                    质量状态             时间          操作
v3        验证中      检索质量检查      1.0 · v1... · 768      正式评测已通过       22:39         查看验证
v2        构建失败    索引构建          历史配置不可追溯        —                    9/6           重新构建
v1        当前生效    线上生效          历史配置不可追溯        历史版本             9/6           详情

──────────────────────────────────────────────────────────────

正式质量评测                                              [运行正式评测]

报告       版本     评测配置            结果      Recall@10   MRR     nDCG@10   发布用途
qr_102     v3       与当前版本一致      已通过      .93       .79       .84      可用于发布
qr_101     v3       配置已变化          已通过      .91       .77       .82      不可用于发布

──────────────────────────────────────────────────────────────

运行记录

类型             版本     当前阶段             进度      状态       开始时间       耗时
发布验证         v3       检索质量检查         95%       运行中      22:39          1m12s
正式质量评测     v3       已完成               100%      已完成      22:34          4m31s
索引构建         v3       已完成               100%      已完成      22:28          5m52s
```

---

# 36. 最终版本详情页草图

```text
← 索引版本

v3                                                     验证中
iv_eab6a49c11b143e7
当前阶段：检索质量检查

                                                   [重新构建] [···]

构建完成 → 正式评测 → 发布验证 → 待激活 → 当前生效
   ✓          ✓           ●          ○          ○

──────────────────────────────────────────────────────────────

版本信息                              正式质量评测

Parser       1.0                     结果          已通过
Chunk        v1-700-100              Recall@5      0.86
Embedding    text2vec-base-chinese   Recall@10     0.93
维度         768                     MRR           0.79
Documents    1,283                   nDCG@10       0.84
Chunks       8,271                   报告          qr_xxx [查看]

──────────────────────────────────────────────────────────────

发布验证

✓ 完整性检查          ✓ 技术检查           ● 检索质量检查
  4 / 4                 5 / 5                进行中

                                                [查看完整验证报告]

──────────────────────────────────────────────────────────────

发布记录

配置快照          已记录
质量报告          已通过
验证报告          验证中
线上激活          尚未执行

                                                   [查看详情]

──────────────────────────────────────────────────────────────

相关运行记录
...
```

---

# 37. 空状态

## 无正式质量报告

```text
正式质量评测

缺少可用于发布的质量报告
当前版本尚未完成符合发布要求的正式检索评测。

[运行正式评测]
```

---

# 38. 正式评测未通过

```text
正式质量评测

质量评测未通过
2 项指标未达到发布要求。

Recall@5     0.72 / 要求 ≥ 0.80
MRR          0.68 / 要求 ≥ 0.75

[查看报告] [重新评测]
```

---

# 39. 发布验证等待质量报告

```text
发布验证

完整性检查        未开始
技术检查          未开始
检索质量检查      等待前置条件

请先完成当前版本的正式质量评测。

[运行正式评测]
```

---

# 40. 验证未通过

```text
发布验证

验证未通过
2 项检查需要处理。

✓ 完整性检查
× 技术检查
✓ 检索质量检查

[查看问题] [重新验证]
```

不要展示：

```text
validation_failed
```

---

# 41. 历史版本兼容

历史迁移版本可能缺少：

```text
完整 Index Definition
Config Fingerprint
Build Fingerprint
正式质量报告
Validation Report
Release Fingerprint
```

这些情况不能展示为系统错误。

统一使用：

```text
历史版本
部分治理信息未记录
```

示例：

```text
配置
历史配置不可完整追溯

质量评测
历史版本 · 无正式质量报告

发布记录
部分信息未记录
```

详情中再解释原因。

---

# 42. 前端实现建议

状态映射集中管理。

不要在组件中散落：

```ts
status === 'validation_failed'
  ? '验证失败'
  : ...
```

建议建立：

```ts
INDEX_VERSION_STATUS_META
VALIDATION_GATE_STATUS_META
QUALITY_REPORT_STATUS_META
RUN_STATUS_META
```

结构示例：

```ts
{
  label,
  description,
  tone,
  primaryAction
}
```

---

# 43. Formatter

建立统一方法：

```ts
formatResourceId()
formatFingerprint()
formatDuration()
formatIndexConfigSummary()
formatLegacyInfo()
```

例如：

```ts
formatFingerprint(
  "7b9a15844dd07ffffd65d465ecafb9b58081a76d73a77e1d4fcfdaf830563810"
)
```

输出：

```text
7b9a1584...63810
```

---

# 44. 后端枚举

本次不要修改后端生命周期：

```text
draft
building
validating
ready
active
previous
retired
cleaned

build_failed
validation_failed
```

只修改前端 Presentation Layer。

---

# 45. 审计字段继续保留

以下字段不要删除：

```text
config_fingerprint
release_fingerprint
validation_report_id
quality_report_id
build_id
activated_at
activated_by
```

只是调整展示层级。

---

# 46. 验收标准

## 信息架构

- [ ] 「版本治理」调整为「索引治理」。
- [ ] 资料版本不再出现在索引治理中。
- [ ] 文件上传、文件更新、数据同步任务不再出现在索引治理运行记录。
- [ ] 索引治理内部无二级 Tab。
- [ ] 索引版本、正式质量评测、运行记录同页直接展开。

## 紧凑展示

- [ ] 页面不存在大量空白大卡片。
- [ ] 索引版本使用紧凑表格。
- [ ] 正式质量评测使用紧凑表格。
- [ ] 运行记录使用紧凑表格。
- [ ] 完整 Hash / ID 不在主列表全量展示。
- [ ] 常规桌面分辨率下，首屏至少可看到索引版本主体与正式质量评测区域入口。

## 状态文案

- [ ] 主 UI 不出现 `build_failed`。
- [ ] 主 UI 不出现 `validation_failed`。
- [ ] 主 UI 不出现 `Legacy 未生成`。
- [ ] `validation_failed` 显示为「验证未通过」。
- [ ] `ready` 显示为「待激活」。
- [ ] `active` 显示为「当前生效」。
- [ ] BLOCKED 与 FAILED 有明确区分。

## 发布记录

- [ ] 主页面不再使用「冻结发布证据」。
- [ ] 区域改为「发布记录」。
- [ ] Config Fingerprint 主页面降级为「配置快照 已记录」。
- [ ] 完整配置指纹仅详情可见。
- [ ] 历史版本缺少 Release Fingerprint 时不显示错误。
- [ ] 使用「历史版本 · 部分信息未记录」。

## 发布流程

- [ ] 正式质量评测和发布验证是两个独立阶段。
- [ ] 缺少正式质量报告时引导「运行正式评测」。
- [ ] 发布验证全部通过后版本进入「待激活」。
- [ ] 验证通过不会自动激活。
- [ ] 「激活版本」为独立明确操作。

---

# 47. 本次明确不做

不要顺手扩大范围：

```text
不重写 Index Governance 后端状态机
不修改索引构建算法
不修改正式质量评测计算逻辑
不修改 Config Fingerprint 生成规则
不修改 Release Fingerprint 生成规则
不实现新的 Sync Pipeline 后端
不增加验证报告 Tab
不增加发布记录 Tab
不增加大面积 Dashboard KPI
不增加无业务价值图表
```

---

# 48. Claude Code 推荐执行顺序

## Step 1

梳理现有：

```text
索引治理组件
路由
状态枚举
任务类型
版本详情组件
正式质量评测相关组件
验证报告组件
```

---

## Step 2

将：

```text
版本治理
```

重命名为：

```text
索引治理
```

移除或迁移：

```text
资料版本
文件上传
文件更新
数据同步
```

---

## Step 3

移除索引治理内部二级 Tab。

页面改成：

```text
发布流程
↓
当前索引定义
↓
索引版本
↓
正式质量评测
↓
运行记录
```

---

## Step 4

重构索引版本表格：

```text
版本
状态
当前阶段
配置摘要
质量状态
创建时间
操作
```

增加统一状态映射。

---

## Step 5

正式质量评测直接加入主页面。

复用已有数据和 API。

不要修改评测业务逻辑。

---

## Step 6

重构版本详情页：

```text
发布流程
↓
版本信息 + 正式质量评测
↓
发布验证
↓
发布记录
↓
相关运行记录
```

---

## Step 7

将：

```text
冻结发布证据
```

改为：

```text
发布记录
```

将完整 Hash 和技术 ID 下沉到详情。

---

## Step 8

统一优化：

```text
Legacy
Failed
Blocked
Ready
Active
Fingerprint
Report Binding
```

对应 UI 文案。

---

## Step 9

整体收紧视觉密度：

```text
section gap
table row
card padding
description spacing
```

删除重复标题和说明。

---

## Step 10

回归以下场景：

```text
v1：历史迁移 + active

v2：build_failed

v3：validating

ready

validation_failed

无正式质量报告

正式质量报告配置不匹配

正式质量评测失败

三层发布验证全部通过

Activate 后：
新版本 → active
旧版本 → previous

历史版本缺少 release fingerprint
```

---

# 49. 最终页面必须回答的 4 个问题

用户进入索引治理后，应当在几秒内回答：

```text
1. 当前线上使用哪个索引版本？
2. 新版本现在进行到哪一步？
3. 为什么它现在还不能发布？
4. 下一步应该执行什么操作？
```

所有：

```text
Fingerprint
Report ID
Build ID
Legacy 原始字段
```

继续保留用于审计、排障与追溯，但不再主导页面视觉。
