# 无系统“未分类”与分类失败治理实施计划

> **供 Claude Code 执行：** 按第 1～7 步连续实施；每步遵循测试先行（RED → GREEN），不要逐步等待用户回复“继续”。仅在真实阻塞或重大范围变化时暂停。

**目标：** 删除知识库级系统“未分类”，允许资料暂时没有分类，并以独立状态治理自动分类失败、重试、人工归类和检索边界。

**架构：** 分类字典只保存真实、名称非空的业务分类；Document 与当前版本 Chunk 的 `category_id/category` 允许为空。`pending/review_required/failed` 是分类处理状态，不创建同名伪分类；分类失败不阻断上传、解析、切片、索引和默认检索。

**技术栈：** FastAPI、Pydantic、PostgreSQL/pgvector、React、TypeScript、Vitest。

## 全局约束

- PostgreSQL 是分类治理运行时；非 PostgreSQL 模式保留兼容能力，但不提供完整治理。
- 分类名称去除首尾空格后不能为空；同一知识库内忽略大小写唯一。
- 新建知识库不再自动创建系统“未分类”。
- “未分类”恢复为普通分类名称，可由管理员自行创建、编辑、停用或删除。
- 自动分类失败时 `category_id = null`、`category = null`，不保存“分类失败”或“未分类”作为分类名。
- 分类失败不阻断上传、解析、切片和索引；ACL、有效期、文档状态和索引版本规则保持不变。
- “全部分类”检索包含无分类资料；指定 `category_ids` 时不包含无分类资料。
- 分类变化必须在同一事务更新 Document 与当前版本 Chunk；历史 Chunk 保留历史快照且不进入当前检索。
- 迁移不修改正文、Embedding 或索引版本，不要求向量重建。
- 页面首次加载不增加整页 Loading；重试、归类等操作使用局部状态与进度反馈。
- 仅执行 Backend 专项 Pytest/Ruff、Frontend Test/ESLint/TypeScript 和 `git diff --check`；不执行完整验证、生产构建、容器构建或 K8s 演练。
- 完成后只保留本地修改；未收到“提交代码”指令不得 Commit 或 Push。

---

## 第 1 步：Schema 与分类状态模型

**文件：**

- 新建：`backend/migrations/0016_nullable_document_category.sql`
- 修改：`backend/app/config.py`
- 修改：`backend/app/schemas.py`
- 修改：`frontend/src/types.ts`
- 测试：`backend/tests/test_postgres_foundation.py`

**实现：**

- Schema 版本升级为 V16，迁移必须幂等。
- Document JSON Metadata 和当前 Chunk Metadata 允许：
  - `category_id: null`
  - `category: null`
- 保留分类状态：`pending / auto_assigned / review_required / manual / failed`。
- 增加失败字段：
  - `classification_failure_code`
  - `classification_failure_reason`
  - `classification_failed_at`
  - `classification_retry_count`
  - `classification_next_retry_at`
- 稳定错误码：
  - `MODEL_UNAVAILABLE`
  - `MODEL_TIMEOUT`
  - `INVALID_RESPONSE`
  - `CATEGORY_NOT_FOUND`
  - `CATEGORY_INACTIVE`
  - `NO_ACTIVE_CATEGORY`
  - `UNKNOWN_ERROR`

**TDD：**

- [ ] 添加 V16 迁移、状态字段和幂等测试。
- [ ] 在隔离的 `TEST_DATABASE_URL` 执行测试，确认旧代码 RED。
- [ ] 实现最小 Schema 与类型变化。
- [ ] 重跑专项测试，确认 GREEN。

**验收：** 数据结构可以明确区分“没有分类”和“分类处理状态”，不再依赖特殊分类行。

---

## 第 2 步：知识库与分类字典规则

**文件：**

- 修改：`backend/app/postgres_repositories.py`
- 修改：`backend/app/knowledge_bases.py`
- 修改：`backend/app/main.py`
- 修改：`backend/migrations/0016_nullable_document_category.sql`
- 测试：`backend/tests/test_postgres_foundation.py`
- 测试：`backend/tests/test_category_templates_api.py`

**实现：**

- 删除 Repository 初始化、新建知识库和迁移中的系统“未分类”创建逻辑。
- 应用模板时只复制有效模板项；关闭模板时允许知识库分类列表为空。
- 分类名称校验：`name.strip()` 后不能为空，`normalized_name = name.casefold()`，同知识库唯一。
- 删除对“未分类”名称的保留限制；它与其他普通分类遵循相同治理规则。
- 删除分类时：被资料引用仍返回 `409 CATEGORY_IN_USE`；无引用时允许删除。
- 删除现有 `is_system` 特权分支；字段如需兼容可暂时保留，但新数据恒为 `false`，后续再独立清理列。

**TDD：**

- [ ] 添加“新知识库没有系统分类”“空模板仍可创建”“普通未分类可创建和删除”失败测试。
- [ ] 确认 RED 后修改 Repository/API。
- [ ] 运行知识库与分类模板专项测试，确认 GREEN。

**验收：** 新知识库不产生隐式分类，分类字典只包含管理员明确维护或模板复制的真实分类。

---

## 第 3 步：自动分类失败与重试服务

**文件：**

- 修改：`backend/app/postgres_documents.py`
- 修改：现有 `DocumentClassifier` 所在模块；若当前未独立拆分，则新建 `backend/app/document_classifier.py`
- 修改：`backend/app/main.py`
- 修改：`backend/app/schemas.py`
- 测试：现有分类测试文件；若职责混杂则新建 `backend/tests/test_document_classification.py`

**实现：**

- 分类开始：状态写入 `pending`，分类字段保持为空。
- 高置信成功：写入真实 `category_id/category`，状态 `auto_assigned`。
- 低置信结果：保持分类为空，保存 `suggested_category_id/confidence`，状态 `review_required`。
- 异常或无有效分类：保持分类为空，状态 `failed`，保存稳定失败码、原因、时间和重试次数。
- 可重试：`MODEL_UNAVAILABLE / MODEL_TIMEOUT / UNKNOWN_ERROR`；使用有限退避和最大次数。
- 不自动重试：`INVALID_RESPONSE / CATEGORY_NOT_FOUND / CATEGORY_INACTIVE / NO_ACTIVE_CATEGORY`。
- 增加单份和批量重新分类接口；重新分类先回到 `pending`，成功后清除全部失败字段。
- 模型不得创建分类；失败不得影响已完成的上传、解析、切片与索引结果。

**TDD：**

- [ ] 覆盖成功、低置信、无分类、超时、无效响应、停用分类、重试耗尽。
- [ ] 确认测试在旧逻辑下 RED。
- [ ] 实现状态机和接口。
- [ ] 重跑分类专项测试，确认 GREEN。

**验收：** 任意模型失败都有稳定状态与可追踪原因，资料主体处理链路不回滚。

---

## 第 4 步：Document、Chunk 原子同步与人工治理

**文件：**

- 修改：`backend/app/postgres_repositories.py`
- 修改：`backend/app/postgres_documents.py`
- 修改：`backend/app/main.py`
- 测试：`backend/tests/test_postgres_foundation.py`
- 测试：现有检索权限测试文件

**实现：**

- 人工确认、修正和批量归类在同一事务更新 Document 与当前活动版本 Chunk。
- 人工归类后状态改为 `manual`，清空建议分类、置信度和失败信息。
- 支持把资料重新置为“无分类”：清空分类字段，状态改为 `pending`；该动作不是创建“未分类”。
- 当前版本 Chunk 同步最新分类；历史 Chunk 不改写，继续受版本隔离。
- 分类停用不清空历史资料归属，但不参与新资料自动分类。

**TDD：**

- [ ] 添加单份归类、批量归类、清空分类、事务失败回滚和历史 Chunk 隔离测试。
- [ ] 确认 RED 后实现事务更新。
- [ ] 在隔离 PostgreSQL 中运行专项测试，确认 GREEN。

**验收：** 分类状态变化在下一次查询立即生效，不出现 Document 与当前 Chunk 分类漂移。

---

## 第 5 步：检索边界与响应解释

**文件：**

- 修改：`backend/app/postgres_documents.py`
- 修改：`backend/app/schemas.py`
- 修改：`backend/app/main.py`
- 测试：`backend/tests/test_retrieval_access.py`
- 测试：`backend/tests/test_hybrid_retrieval.py`

**实现：**

- 未指定分类过滤：Vector、Keyword、Metadata 三路均允许 `category_id IS NULL` 的当前可检索资料。
- 指定 `category_ids`：三路仅召回匹配 ID，无分类资料不参与候选、精排、Prompt 和 Citation。
- 兼容名称过滤 `categories`：先解析为真实分类 ID；不得把状态文案解析为分类。
- 查询响应增加可解释统计：无分类候选数量、分类过滤匹配数量。
- 结果为空时区分：知识库无资料、分类无匹配、ACL 后无可见资料、资料仍在处理。
- ACL、删除、失效、当前版本条件必须在候选召回前继续生效。

**TDD：**

- [ ] 添加“全部分类包含无分类”“指定分类排除无分类”“三路一致”“ACL 不回退”失败测试。
- [ ] 确认 RED 后修改过滤构造与响应。
- [ ] 运行 Retrieval/ACL 专项测试，确认 GREEN。

**验收：** 无分类状态不会造成数据消失，也不会绕过用户明确指定的分类边界。

---

## 第 6 步：知识库详情与问答页面

**文件：**

- 修改：`frontend/src/components/DocumentPanel.tsx`
- 修改：`frontend/src/components/KnowledgeBaseDetailPage.tsx`
- 修改：问答工作台对应组件
- 修改：`frontend/src/api.ts`
- 修改：`frontend/src/types.ts`
- 修改：`frontend/src/styles.css`
- 测试：`frontend/src/App.test.tsx`

**实现：**

- 分类管理删除系统“未分类”固定行和保护按钮；空列表显示“暂无分类”。
- 允许管理员创建普通名称“未分类”；空名称提交显示明确错误。
- 资料分类列：有分类显示名称，无分类显示“—”。
- 独立状态显示：待分类、自动分类、待确认、人工归类、分类失败。
- 失败状态展示简短原因，并提供“重新分类”“人工归类”。
- 筛选增加“无分类”“分类失败”；批量操作支持重新分类和人工归类。
- 问答分类下拉只展示真实分类字典，默认“全部分类”；不得展示状态文案作为分类选项。
- 操作过程中使用行级或局部进度；页面首次加载不增加整页 Loading。

**TDD：**

- [ ] 添加空分类列表、普通“未分类”、无分类资料展示、失败重试、批量归类和问答筛选测试。
- [ ] 运行相关 Vitest，确认 RED。
- [ ] 实现最小页面和接口变更。
- [ ] 重跑相关 Vitest，确认 GREEN。

**验收：** 用户能明确看出“没有分类”和“分类失败”的区别，并能在资料列表完成闭环治理。

---

## 第 7 步：迁移、兼容与轻量验收

**文件：**

- 修改：`backend/migrations/0016_nullable_document_category.sql`
- 修改：`backend/tests/test_postgres_foundation.py`
- 修改：必要的前后端专项测试
- 更新：与分类规则直接相关的项目文档

**迁移规则：**

- 删除 `is_system = true` 且名称为“未分类”的分类记录。
- 原来引用系统“未分类”的 Document：清空 `category_id/category`。
- 当前活动版本 Chunk 同步清空分类字段；历史 Chunk 保留历史快照。
- 原状态为 `failed` 的资料保留失败状态；其他原系统未分类资料转换为 `pending`。
- 用户创建的普通同名分类不删除；以 `is_system` 和稳定 ID 判断，不只按名称判断。
- 迁移重复执行结果一致，不产生新分类、不修改正文和 Embedding。

**轻量验证：**

- [ ] 在隔离 PostgreSQL 执行 V1→V16 和 V15→V16 迁移测试。
- [ ] 运行分类、自动分类、Retrieval、ACL、知识库创建相关 Backend 专项 Pytest。
- [ ] 运行 `ruff check` 覆盖本轮 Backend 文件。
- [ ] 运行 Frontend 相关 Vitest、ESLint、`tsc --noEmit`。
- [ ] 运行 `git diff --check`。
- [ ] 启动现有本地项目，检查知识库创建、空分类、上传、分类失败、重试、人工归类和问答分类筛选真实页面。
- [ ] 不执行完整验证、生产构建、容器构建或 K8s 演练。

## 最终放行条件

- 新知识库不自动产生系统“未分类”。
- 分类字典中的每个分类名称均非空且知识库内唯一。
- 自动分类失败不阻断资料上传、解析、切片和索引。
- 分类失败资料有稳定失败码、有限重试和人工归类入口。
- “全部分类”可以检索无分类资料；指定分类不能召回无分类资料。
- Vector、Keyword、Metadata 与 ACL 边界一致。
- Document 与当前 Chunk 分类原子同步。
- 页面不再把“待分类”“分类失败”冒充真实分类。
- 所有轻量质量门通过，且只保留本地修改等待用户检查和“提交代码”。
